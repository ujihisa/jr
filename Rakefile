# -*- ruby -*-

require 'rubygems'
require 'hoe'

HOE = Hoe.spec 'jr' do
  developer('Tatsuhiro Ujihisa', 'ujihisa at gmail com')
  self.readme_file   = 'README.md'
  #self.history_file  = 'CHANGELOG.rdoc'
  #self.extra_rdoc_files  = FileList['*.rdoc']
  self.extra_dev_deps << ['nokogiri', '>= 0']
  self.extra_dev_deps << ['termcolor', '>= 0']
  #self.spec_extras = { :extensions => ["ext/stree/extconf.rb"] }
end

# vim: syntax=ruby
